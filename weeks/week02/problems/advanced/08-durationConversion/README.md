# 08 執行時間換算

- 難度：進階

設備以總秒數記錄執行時間。換算成小時、分鐘與秒數，分鐘與秒數必須介於 0～59。

## 輸入格式

輸入非負整數 `totalSeconds`，範圍為 0～1000000000，請使用 `long long`。

## 輸出格式

依序輸出 `Hours`、`Minutes`、`Seconds`，每項一行，格式為 `名稱: 數值`。

## 範例輸入
```text
7384
```
## 範例輸出
```text
Hours: 2
Minutes: 3
Seconds: 4
```
