# 05 三次量測平均

- 難度：基礎

讀取三次量測值並輸出平均，格式為 `Average: 平均值`，固定保留兩位小數。

## 輸入格式

輸入三個 0～10000 的非負數值，每個最多三位小數。

## 範例輸入
```text
12.5 15 14
```
## 範例輸出
```text
Average: 13.83
```

可使用 `<iomanip>` 中的 `std::fixed` 與 `std::setprecision(2)`。
